<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Team_project"
        tools:targetApi="34">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDFlomI7ZzeV285jA16qdKGZiORopoDL1s"/>

        <activity
            android:name=".Toolbar.IntroActiviy"
            android:exported="true"> <!-- Android 12 이상에서는 exported 속성 추가 필요 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".SignUpActivity">
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".Chat.ChatActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".AddressPickerActivity"
            android:exported="true">
        </activity>

        <activity android:name=".Environment.Store.Payment.PaymentFragment">
            <!-- 필수 속성들을 추가할 수 있습니다 -->
        </activity>

        <activity android:name=".Environment.Store.Payment.DeliveryDestinationActivity" />

        <activity android:name=".Environment.Store.Payment.PaymentCompleteActivity" />

    </application>
</manifest>
